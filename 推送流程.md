# 论坛项目日常开发与更新指南 📘

本文档记录了从“本地写代码”到“服务器上线”的完整操作流程。

## 1. 本地开发 (Windows) 💻

当您在本地电脑上修改了代码（例如修改了 Vue 页面或 Python 接口）后，请按照以下步骤将代码保存并上传：

1.  **打开终端**：
    在 VS Code 中按下 `Ctrl + ~` 打开终端。

2.  **提交代码**：
    运行以下三行命令（一行一行运行）：

    ```powershell
    # 1. 将所有修改加入暂存区
    git add .

    # 2. 提交修改说明 (引号里写您改了什么)
    git commit -m "更新说明：例如修改了首页样式"

    # 3. 推送到 Gitee 仓库
    git push gitee main
    ```

    > **提示**：如果 `git push` 提示冲突或失败，请先运行 `git pull gitee main` 拉取最新代码，解决冲突后再推送。

---

## 2. 服务器更新 (Linux) 🚀

当您确认代码已经成功推送到 Gitee 后，登录您的 Linux 服务器执行更新。

1.  **登录服务器**：
    使用 SSH 登录您的 CentOS 服务器。

2.  **执行一键更新**：
    直接运行我们在服务器上配置好的脚本：

    ```bash
    sudo bash /var/www/luntan/update_server.sh
    ```

    > **脚本会自动完成以下工作**：
    > *   📥 从 Gitee 拉取最新代码
    > *   🐍 更新 Python 后端依赖 (pip)
    > *   🎨 重新构建 Vue 前端 (npm build)
    > *   🔄 重启后端服务

3.  **验证**：
    当看到终端输出 `✅ 更新完成！` 时，刷新浏览器即可看到最新效果。

---

## 3. 常见问题排查 ❓

### Q: 服务器运行脚本提示 "Permission denied"？
**A**: 请使用 `sudo bash` 来运行脚本，或者给脚本赋予执行权限：
```bash
sudo chmod +x /var/www/luntan/update_server.sh
/var/www/luntan/update_server.sh
```

### Q: 本地 git push 慢或者卡住？
**A**: 检查网络，或者确认远程仓库地址是否为 Gitee：
```powershell
git remote -v
# 应该看到 gitee https://gitee.com/...
```
